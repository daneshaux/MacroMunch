@* Usage: <TagChip Text="Vegan" Kind="diet" /> *@
<span class="tag-chip @(Kind)">
    @Text
</span>

@code {
    [Parameter] public string Text { get; set; } = "";
    [Parameter] public string? Kind { get; set; } // "diet" | "style" | "spice" (optional)
}